# 数据坊 (DataForge) — 解决方案

> 目标：做一个通用的桌面端数据集管理工具，先满足缺陷标注数据集的浏览/统计/质检/导出需求，后续可扩展集成到其他平台。

---

## 一、技术栈

| 项目 | 选型 |
|------|------|
| 操作系统 | Windows 11（开发环境） |
| 编程语言 | Python 3.11 |
| 包管理 | pip + venv 虚拟环境 |
| GUI 框架 | PyQt6 |
| 图像处理 | Pillow + OpenCV |
| 数据分析 | numpy / pandas |
| 图表 | pyqtgraph |
| 配置文件 | PyYAML |
| 后台任务 | QThread + concurrent.futures |
| 打包（v1 后） | PyInstaller |

### 后期可选库（按需安装）

| 库 | 用途 |
|----|------|
| imagehash | 图像去重（pHash） |
| scikit-learn | train/val/test 划分 |
| pycocotools | COCO 格式读写 |
| labelme2yolo | LabelMe → YOLO 转换 |
| torch / ultralytics | AI 辅助标注 |

---

## 二、设计原则

1. **业务与界面解耦** — `core/` 层完全不依赖 PyQt，将来要接 Web/CLI/其他平台时核心代码可直接复用
2. **不绑定数据集** — 工具是通用的，启动时让用户选择数据集目录，不写死路径
3. **支持多种标注格式** — 优先 LabelMe，逐步扩展到 COCO、YOLO 等
4. **不过度设计** — 先实现核心功能，扩展性以后再说
5. **样式三层架构** — Tokens (gui/theme.py) → 语义组件 (qfluentwidgets Label / 项目封装的 chips) → 一份 app.qss。视图层和 widgets 层禁止写颜色字面量、禁止 inline `setStyleSheet`，新主题只需新建一个 `Tokens` 实例并在 `THEMES` 注册

---

## 三、产品定位

这不是一个"看图工具"，而是一个**本地数据集管理平台**。参照业内同类产品（Roboflow / FiftyOne / CVAT / Supervisely / Label Studio）的工作流：
**导入 → 浏览/审阅 → 清洗/处理 → 划分 → 导出**。

每一步都是工具该承担的功能模块，而不是几个孤立的"页面"。"概览"不是一个独立的导航项，它是当前数据集的属性面板，跟着数据集走。

---

## 四、目录结构

```
defect_dataset_tool/
│
├── core/                       # 业务逻辑层（纯 Python，无 UI 依赖）
│   ├── __init__.py
│   ├── models.py               # 数据模型（Dataset / Category / Image / Annotation）
│   ├── dataset.py              # 数据集扫描（两阶段：索引 → 标注计数）
│   ├── annotation.py           # LabelMe JSON 解析（容错）
│   ├── index_cache.py          # SQLite 索引缓存
│   ├── stats.py                # 统计分析
│   ├── fileops.py              # 重命名/移动/删除等文件操作
│   ├── transform.py            # 旋转/翻转/裁剪/缩放
│   ├── convert.py              # 图片格式转换
│   ├── dedup.py                # 图像去重（pHash）
│   ├── quality.py              # 图像质量检查（模糊/过曝/空白）
│   ├── splitter.py             # train/val/test 划分
│   └── exporter/               # 数据集格式导出器
│       ├── subset.py           # 选区/筛选导出
│       ├── yolo.py             # → YOLO-seg
│       ├── coco.py             # → COCO
│       ├── llava.py            # → LLaVA JSONL
│       └── mvtec.py            # → MVTec 异常检测
│
├── gui/                        # PyQt6 界面层（不允许写颜色字面量 / inline setStyleSheet）
│   ├── __init__.py
│   ├── main_window.py          # 主窗口 + 顶部 app bar + 左侧分组导航
│   ├── theme.py                # 设计令牌 + Light/Dark 主题 + set_theme()
│   ├── styles/
│   │   └── app.qss             # 唯一一份 QSS，{TOKEN} 占位
│   ├── views/                  # 一个工作流模块 = 一个 view
│   │   ├── empty_state_view.py # 未打开数据集时的空状态（CTA + 最近列表）
│   │   ├── overview_view.py    # 数据集 / 概览（属性面板）
│   │   ├── browser_view.py     # 数据集 / 浏览
│   │   ├── detail_view.py      # 数据集 / 浏览 → 单图详情（嵌套，不占 nav 位）
│   │   ├── quality_view.py     # 数据处理 / 质量检查
│   │   ├── dedup_view.py       # 数据处理 / 重复检测
│   │   ├── transform_view.py   # 数据处理 / 批量变换
│   │   ├── split_view.py       # 数据处理 / 数据集划分
│   │   └── export_view.py      # 导出（统一入口，内含格式选择）
│   ├── widgets/
│   │   ├── nav_menu.py         # 分组可折叠导航树（封装 NavigationInterface）
│   │   ├── dataset_chip.py     # 顶栏当前数据集胶囊
│   │   ├── thumbnail_grid.py
│   │   ├── image_viewer.py
│   │   ├── category_tree.py
│   │   ├── chips.py            # FilterChip / GhostButton
│   │   └── stats_chart.py
│   ├── dialogs/
│   │   └── op_dialogs.py       # 全部继承 qfluentwidgets.MessageBoxBase
│   └── workers/                # 后台 QThread
│       ├── scan_worker.py
│       ├── thumbnail_worker.py
│       └── batch_worker.py
│
├── tests/
├── config/
│   └── default_config.yaml
├── main.py
├── requirements.txt
└── 解决方案.md
```

---

## 五、信息架构（左侧导航分组）

整个左侧导航是一个 **可折叠的分组菜单**，按工作流阶段组织。**未打开数据集前，整个菜单 disabled**，强制用户先从顶栏 [打开数据集] 进入。

```
📂 数据集
   ├ 概览              ← 默认落地页（属性面板：统计 + 类别分布 + 完整率）
   ├ 浏览              ← 缩略图网格 + 单图详情（详情通过双击进入，不占 nav 位）
   └ 类别管理          ← 类别重命名/合并/拆分

🔧 数据处理
   ├ 质量检查          ← 模糊 / 过曝 / 空白 / 损坏
   ├ 重复检测          ← pHash 找重复 / 近似图
   ├ 批量变换          ← 旋转 / 翻转 / 裁剪 / 缩放 / 重命名 / 格式转换
   └ 数据集划分        ← train / val / test，按比例或按类别均衡

📤 导出
   └ 导出向导          ← 单一入口，内部 step：选数据 → 选格式 (YOLO/COCO/LLaVA/MVTec) → 输出

⚙ 设置                ← 主题、缓存目录、最近列表管理
```

**关键决定**：
1. **「概览」属于「数据集」组**，不是独立顶级项。打开数据集后默认落到这里。
2. **「单图详情」不占 nav 位**，是「浏览」内部的子页面（QStackedWidget 切换）。
3. **「导出」收敛为单一向导**，不为每种格式开一个 nav 项 — 否则 nav 会被 4 个格式塞满。
4. **「批量变换」整合所有图像处理**（rotate/flip/crop/resize/rename/convert），用左侧操作面板 + 右侧预览的布局，而不是每个操作一个对话框。

---

## 六、顶部 App Bar

```
┌─────────────────────────────────────────────────────────────────┐
│ 📷 数据集管理工具  [📂 打开数据集 ▾]  [🏷 Loose · D:\…\dataset]  [?] [⚙] │
└─────────────────────────────────────────────────────────────────┘
```

- **App icon + 名称**：左上角，固定
- **打开数据集（带下拉）**：主操作按钮，下拉菜单显示「最近打开」列表
- **当前数据集 chip**：胶囊形式显示数据集名 + 路径（中段省略），全程可见
- **? 帮助**：弹快捷键速查表
- **⚙ 设置**：主题切换、缓存管理

---

## 七、UI 草图（修订版 — 数据管理平台风格）

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 📷 数据集管理工具   [📂 打开数据集 ▾]  [🏷 Loose · D:\…\dataset]  [?] [⚙] │
├────────────────┬─────────────────────────────────────────────────────────┤
│                │  概览                                                    │
│ 📂 数据集    ▾ │  Loose 数据集 · 标准布局 · 5,102 张                      │
│   • 概览       │  ──────────────────────────────────────────────────     │
│   ◦ 浏览       │  ┌─────────┬─────────┬─────────┬─────────┐             │
│   ◦ 类别管理   │  │ 总图片  │ 总标注  │ 类别数  │ 未标注  │             │
│                │  │ 5,102   │ 7,612   │  15     │  196 ⚠  │             │
│ 🔧 数据处理  ▾ │  └─────────┴─────────┴─────────┴─────────┘             │
│   ◦ 质量检查   │                                                          │
│   ◦ 重复检测   │  类别分布                                                │
│   ◦ 批量变换   │  ████████████ Loose         2212                         │
│   ◦ 数据集划分 │  ██████ Lose                1138                         │
│                │  ███ Fracture                634                         │
│ 📤 导出      ▸ │  ...                                                     │
│                │                                                          │
│ ⚙ 设置         │                                                          │
└────────────────┴─────────────────────────────────────────────────────────┘
```

**浏览页（带类别树 + 缩略图 + 选区操作栏）：**

```
┌────────────────┬─────────────────────────────────────────────────────────┐
│ 📂 数据集    ▾ │  浏览                          [选中 12 项] [批量变换▼] │
│   ◦ 概览       │  ┌──────────┬──────────────────────────────────────┐  │
│ ► • 浏览       │  │ 类别 (15)│ 🔍[搜索]  [全部▼] [已标注] [未标注] │  │
│   ◦ 类别管理   │  │ ▾ 全部   │ ────────────────────────────────────│  │
│                │  │   5,102  │ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐    │  │
│ 🔧 数据处理  ▸ │  │   Loose  │ │🖼│ │🖼│ │🖼│ │🖼│ │🖼│ │🖼│    │  │
│ 📤 导出      ▸ │  │   2,212  │ └──┘ └──┘ └──┘ └──┘ └──┘ └──┘    │  │
│                │  │   Lose   │ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐    │  │
│                │  │   1,138  │ │🖼│ │🖼│ │🖼│ │🖼│ │🖼│ │🖼│    │  │
│                │  │   ...    │ └──┘ └──┘ └──┘ └──┘ └──┘ └──┘    │  │
│                │  └──────────┴──────────────────────────────────────┘  │
│                │  共 5,102 张  · 第 1-60 / 86 页  [< 1 2 3 ... 86 >]   │
└────────────────┴─────────────────────────────────────────────────────────┘
```

**空状态（未打开数据集）：**

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 📷 数据集管理工具   [📂 打开数据集 ▾]                          [?] [⚙]  │
├────────────────┬─────────────────────────────────────────────────────────┤
│ 📂 数据集    ▾ │                                                          │
│   ◦ 概览  (灰)│              📂                                          │
│ 🔧 数据处理 ▸ │           还没有打开数据集                                │
│  (整组灰)     │                                                          │
│ 📤 导出     ▸ │     [打开数据集]                                         │
│                │                                                          │
│                │     最近打开                                             │
│                │     • Loose 数据集    D:\KuaKe-download\…  3 天前       │
│                │     • mvtec_bottle    D:\datasets\mvtec…   1 周前       │
└────────────────┴─────────────────────────────────────────────────────────┘
```

---

## 八、功能清单（按模块组织）

### 🔴 v1 - P0 必须有

| 模块 | 功能 | 说明 |
|------|------|------|
| 顶部 | 打开数据集 + 最近列表 | 主入口，支持拖放 |
| 顶部 | 当前数据集 chip | 全程可见 |
| 数据集 / 概览 | 统计卡片 + 类别分布图 + 完整率 | 默认落地页 |
| 数据集 / 浏览 | 类别树 + 缩略图网格 + 筛选 + 分页 | 双击进详情 |
| 数据集 / 浏览 → 详情 | 缩放/拖动/标注叠加/H 键切换/A·D 翻页/Esc 返回 | |
| 空状态 | 未打开数据集时的引导页 + 最近列表 | |

### 🟠 v2 - P1 数据处理 + 导出（核心代码已就绪，需 GUI）

| 模块 | 功能 | core/ 状态 |
|------|------|-----------|
| 数据处理 / 质量检查 | 模糊/过曝/空白/损坏 | quality.py 已有 |
| 数据处理 / 重复检测 | pHash 找重复，按相似度分组 | dedup.py 已有 |
| 数据处理 / 批量变换 | 旋转/翻转/裁剪/缩放/重命名/格式转换 | transform.py + convert.py 已有 |
| 数据处理 / 数据集划分 | train/val/test，按比例 or 类别均衡 | splitter.py 已有 |
| 导出 / 向导 | 选数据 → 选格式 (YOLO/COCO/LLaVA/MVTec) → 输出 | exporter/* 已有 |
| 数据集 / 类别管理 | 重命名/合并/拆分类别 | fileops.py 部分有 |
| 设置 | 主题、缓存路径、最近列表 | 新增 |

### 🟡 v3 - P2 智能化

10. AI 辅助预标注（SAM2 / YOLO 预标）
11. 相似图搜索（DINOv2 + FAISS）
12. Bad case 收集
13. 多用户标注协作（如有需要才做）

---

## 九、数据模型设计

```python
# core/models.py（核心数据模型示意）

@dataclass
class Dataset:
    name: str
    root_path: Path
    categories: list[Category]
    total_images: int
    total_annotations: int

@dataclass
class Category:
    name: str
    image_count: int
    label_count: int          # 已标注数
    images: list[ImageInfo]

@dataclass
class ImageInfo:
    path: Path                # 图片绝对路径
    category: str
    width: int
    height: int
    has_label: bool
    label_path: Path | None

@dataclass
class Annotation:
    image_path: Path
    shapes: list[Shape]

@dataclass
class Shape:
    label: str
    shape_type: str           # polygon / rectangle / point
    points: list[tuple[float, float]]
```

---

## 十、关键流程

### 流程 1：打开数据集

```
用户点击"打开"
   ↓
弹出文件夹选择对话框
   ↓
gui/workers/scan_worker.py 启动后台线程
   ↓
core/dataset.py::Dataset.scan(path) 扫描文件系统
   ├── 遍历每个类别文件夹
   ├── 收集 images/ 和 labels/ 文件
   ├── 解析 LabelMe JSON
   └── 构建 Dataset 对象
   ↓
缓存索引到本地 SQLite
   ↓
信号回到主线程，刷新 UI
```

### 流程 2：浏览图片

```
用户点击类别树某一项
   ↓
core/dataset.py::get_images_by_category()
   ↓
gui/widgets/thumbnail_grid.py 加载缩略图（懒加载）
   ├── 读取缓存或后台生成
   └── 显示状态角标
   ↓
用户双击 → 打开 detail_view
   ├── 加载原图
   └── 解析对应 JSON 叠加绘制标注
```

---

## 十一、开发顺序（建议）

| 阶段 | 内容 | 预计 |
|------|------|------|
| Day 1 上午 | 环境搭建（venv + 依赖）+ 项目骨架 + core/models.py + core/dataset.py | 半天 |
| Day 1 下午 | core/annotation.py + core/stats.py + 单元测试 | 半天 |
| Day 2 上午 | 主窗口 + 菜单栏 + 打开对话框 + 类别树 | 半天 |
| Day 2 下午 | 概览页 + 统计图表 | 半天 |
| Day 3 上午 | 图片浏览器 + 缩略图网格 + 后台缓存 | 半天 |
| Day 3 下午 | 单图详情 + 标注绘制 + 导出功能 | 半天 |
| **完成** | **v1 Demo 跑通** | **3 天** |

---

## 十二、扩展性预留

虽然本期不做，但代码层面预留这些扩展点：

1. **core/ 完全独立** — 不 import 任何 PyQt 模块
2. **数据模型用 dataclass** — 方便序列化为 JSON / Pydantic
3. **导出器接口化** — `core/exporter/` 下每个格式一个文件，加新格式不改其他代码
4. **配置外置** — 数据集路径、缓存位置等都走 config.yaml
5. **后台任务统一封装** — 所有耗时操作都通过 worker 包装，方便后期换成异步任务队列

未来如果要升级到完整的 API + 插件架构，只需在 `core/` 之外新增 `api/` 和 `plugins/` 目录，**core 代码不需要重写**。

---

## 十三、待确认事项

1. ✅ 技术栈：Python 3.11 + PyQt6
2. ✅ 通用工具，不绑定特定数据集路径
3. ✅ 项目位置：`C:\Users\zq\Desktop\defect_dataset_tool`
4. ⏳ 实际开发时间：等用户决定何时开始
5. ⏳ v1 是否需要打包 exe：未定

---

## 十四、风险与注意点

| 风险 | 应对 |
|------|------|
| 数据集文件多（5000+），扫描慢 | 后台线程 + SQLite 缓存索引 |
| 缩略图加载卡顿 | 懒加载 + 磁盘缓存（diskcache 库） |
| 中文路径 / 中文文件名 | Python 3 原生支持，注意编码 |
| LabelMe JSON 格式变化 | 容错解析，记录解析失败的文件 |
| 标注与图片不配对 | 扫描时检查并标记异常 |
| 大图查看时内存占用高 | 按需加载，不全部读入内存 |

---

## 十五、通用任务类型体系

> 设计目标：**输出格式是已知的格子，输入是任意混乱数据，工具帮用户把数据塞进格子里。**

### 15.1 图像数据集任务类型完整分类

```
图像级（整图一个标签）
├─ 二分类        正常/异常、良品/不良品、猫/狗
├─ 多分类        N 类选 1（ImageNet 式）
├─ 多标签        N 个标签选多个（场景标签）
├─ 异常检测      训练集只有正样本（MVTec AD 式）
├─ 回归          连续值标签（质量评分、角度）
└─ 无标注        纯图片集合（自监督/生成/聚类）

区域级（框/多边形标注）
├─ 目标检测      水平矩形框（YOLO/COCO/VOC）
├─ 旋转框检测    倾斜矩形（DOTA 式，遥感/文字）
└─ 文字检测+识别  文字区域 + 文本内容（ICDAR 式）

像素级（逐像素标注）
├─ 语义分割      每个像素一个类别（mask 图）
├─ 实例分割      每个目标单独 mask（COCO-seg）
└─ 全景分割      语义 + 实例合并

点级（坐标标注）
├─ 关键点/姿态    目标上的关键点坐标（COCO-keypoints）
└─ 密度计数      用点标注统计数量

图像对（成对数据）
├─ 超分辨率      低→高分辨率
├─ 去噪/去模糊    退化→干净
├─ 风格迁移      A 域→B 域（pix2pix）
└─ 图像翻译      source→target
```

### 15.2 TaskType 枚举

`core/task_types.py` 定义 `TaskType` 枚举 + `TaskTypeInfo` 元信息注册表。每种任务类型声明：

- `annotation_level`: image / region / pixel / point / pair
- `needs_shapes`: 是否需要框/多边形/点标注
- `needs_image_label`: 是否需要图像级标签
- `valid_shape_types`: 允许的形状类型
- `export_formats`: 可用的导出格式列表
- `augment_updates_shapes`: 增强时是否需要同步更新标注坐标

### 15.3 各模块影响

| 模块 | 变更 |
|------|------|
| `core/models.py` | ImageInfo 新增 `image_label`, `image_tags` 字段 |
| `core/project.py` | Project 新增 `task_type`, `target_format` 字段 |
| `core/dataset.py` | 不变（扫描是格式无关的） |
| `core/augment.py` | 根据 `augment_updates_shapes` 决定是否更新标注 |
| `core/exporter/` | 根据 `export_formats` 过滤可用格式；新增 ImageFolder/MVTec/Mask-PNG 导出器 |
| `core/quality.py` | 不变（质量检查与任务类型无关） |
| GUI 概览页 | 根据任务类型显示不同的就绪条件 |
| GUI 导出页 | 根据任务类型过滤可用格式 |

### 15.4 支持优先级

| 优先级 | 任务类型 | 状态 |
|--------|---------|------|
| P0 | 目标检测 | ✓ 已支持 |
| P0 | 图像分类 | 待实现 |
| P0 | 异常检测 | 待实现 |
| P1 | 语义分割 / 实例分割 | 待实现 |
| P2 | 关键点检测 / 旋转框 | 待实现 |
| P3 | 图像对 / OCR | 待实现 |

---

**文档版本**：v1.1
**创建日期**：2026-04-07
**最后更新**：2026-04-09
**状态**：开发中

# 故障数据集管理工具 — 解决方案

> 目标：做一个通用的桌面端数据集管理工具，先满足故障标注数据集的浏览/统计/质检/导出需求，后续可扩展集成到其他平台。

---

## 一、技术栈

| 项目 | 选型 |
|------|------|
| 操作系统 | Windows 11（开发环境） |
| 编程语言 | Python 3.11 |
| 包管理 | pip + venv 虚拟环境 |
| GUI 框架 | PyQt6 |
| 图像处理 | Pillow + OpenCV |
| 数据分析 | numpy / pandas |
| 图表 | pyqtgraph |
| 配置文件 | PyYAML |
| 后台任务 | QThread + concurrent.futures |
| 打包（v1 后） | PyInstaller |

### 后期可选库（按需安装）

| 库 | 用途 |
|----|------|
| imagehash | 图像去重（pHash） |
| scikit-learn | train/val/test 划分 |
| pycocotools | COCO 格式读写 |
| labelme2yolo | LabelMe → YOLO 转换 |
| torch / ultralytics | AI 辅助标注 |

---

## 二、设计原则

1. **业务与界面解耦** — `core/` 层完全不依赖 PyQt，将来要接 Web/CLI/其他平台时核心代码可直接复用
2. **不绑定数据集** — 工具是通用的，启动时让用户选择数据集目录，不写死路径
3. **支持多种标注格式** — 优先 LabelMe，逐步扩展到 COCO、YOLO 等
4. **不过度设计** — 先实现核心功能，扩展性以后再说

---

## 三、目录结构

```
defect_dataset_tool/
│
├── core/                       # 业务逻辑层（纯 Python，无 UI 依赖）
│   ├── __init__.py
│   ├── models.py               # 数据模型（Dataset / Image / Annotation）
│   ├── dataset.py              # 数据集扫描、加载、索引
│   ├── annotation.py           # LabelMe JSON 解析
│   ├── stats.py                # 统计分析
│   ├── quality.py              # 图像质量检查（P1）
│   ├── dedup.py                # 图像去重（P1）
│   ├── splitter.py             # 数据集划分（P1）
│   └── exporter/               # 格式转换器
│       ├── yolo.py             # → YOLO-seg
│       ├── coco.py             # → COCO
│       ├── llava.py            # → LLaVA JSONL（大模型）
│       └── mvtec.py            # → MVTec 异常检测
│
├── gui/                        # PyQt6 界面层
│   ├── __init__.py
│   ├── main_window.py          # 主窗口
│   ├── views/
│   │   ├── overview_view.py    # 概览页
│   │   ├── browser_view.py     # 图片浏览器
│   │   └── detail_view.py      # 单图详情
│   ├── widgets/
│   │   ├── thumbnail_grid.py   # 缩略图网格
│   │   ├── image_viewer.py     # 大图查看 + 标注绘制
│   │   ├── category_tree.py    # 类别树
│   │   └── stats_chart.py      # 统计图表
│   └── workers/                # 后台线程
│       ├── scan_worker.py
│       └── thumbnail_worker.py
│
├── tests/                      # 单元测试
│
├── config/
│   └── default_config.yaml     # 配置文件
│
├── main.py                     # 程序入口
├── requirements.txt
└── 解决方案.md                  # 本文档
```

---

## 四、功能清单

### 🔴 v1 - P0 必须有

#### 1. 数据集打开 / 自动扫描
- 启动后选择任意数据集根目录
- 自动识别结构（`类别/images/` + `类别/labels/`）
- 后台线程扫描，缓存索引到本地 SQLite
- 二次打开秒级加载
- 支持最近打开列表

#### 2. 概览 Dashboard
- 总图片数、总标注数、总类别数
- 类别分布柱状图（pyqtgraph）
- 标注完整率（每类已标注/未标注比例）
- 一键刷新

#### 3. 图片浏览器
- 左侧：类别树（带数量徽章）
- 右侧：缩略图网格（支持分页）
- 顶部：搜索框 + 筛选器（已标注/未标注）
- 缩略图右下角状态标：✅有标注 / ⚠️无标注
- 双击进入单图详情

#### 4. 单图详情视图
- 大图显示（滚轮缩放、拖动）
- 标注叠加绘制（多边形/矩形，类别着色）
- 元信息侧栏（文件名、尺寸、类别、标注数量）
- 上一张/下一张快捷键（A/D）

#### 5. 基础统计导出
- 一键导出 CSV/JSON 报表
- 内容：类别分布、标注完整率、文件清单

---

### 🟠 v2 - P1 后续扩展

6. **图像去重**（pHash）
7. **数据质量检查**（模糊、过曝、空白）
8. **train/val/test 划分**（按比例 / 按类别均衡）
9. **格式转换器**：
   - LabelMe → YOLO-seg
   - LabelMe → COCO
   - LabelMe → LLaVA JSONL（VLM 微调用）
   - LabelMe → MVTec 文件夹（异常检测用）

---

### 🟡 v3 - P2 接 AI

10. **AI 辅助预标注**（SAM2 / YOLO 预标）
11. **相似图搜索**（DINOv2 + FAISS）
12. **Bad case 收集**

---

## 五、UI 草图

```
┌─────────────────────────────────────────────────────────────┐
│ 🚂 数据集管理工具          [打开] [导出] [设置]    _ □ × │
├──────────┬──────────────────────────────────────────────────┤
│ 📁 类别  │  🔍 [搜索]  [全部▼] [已标注] [未标注]            │
│          │  ─────────────────────────────────────────────── │
│ ▾ 全部   │  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐  │
│   2212  │  │ 🖼 │ │ 🖼 │ │ 🖼 │ │ 🖼 │ │ 🖼 │ │ 🖼 │  │
│   Loose │  │ ✅ │ │ ✅ │ │ ⚠️ │ │ ✅ │ │ ✅ │ │ ❌ │  │
│   1138  │  └────┘ └────┘ └────┘ └────┘ └────┘ └────┘  │
│   Lose  │                                                    │
│    354  │  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐  │
│ Eyewink │  │ 🖼 │ │ 🖼 │ │ 🖼 │ │ 🖼 │ │ 🖼 │ │ 🖼 │  │
│   ...   │  └────┘ └────┘ └────┘ └────┘ └────┘ └────┘  │
│          │                                                    │
│ ─────── │  共 5102 张  当前 1-30 [< 1 2 3 4 5 ... 171 >]  │
│ 📊 概览  │                                                    │
│ 🖼 浏览  │                                                    │
│ 📤 导出  │                                                    │
└──────────┴──────────────────────────────────────────────────┘
```

---

## 六、数据模型设计

```python
# core/models.py（核心数据模型示意）

@dataclass
class Dataset:
    name: str
    root_path: Path
    categories: list[Category]
    total_images: int
    total_annotations: int

@dataclass
class Category:
    name: str
    image_count: int
    label_count: int          # 已标注数
    images: list[ImageInfo]

@dataclass
class ImageInfo:
    path: Path                # 图片绝对路径
    category: str
    width: int
    height: int
    has_label: bool
    label_path: Path | None

@dataclass
class Annotation:
    image_path: Path
    shapes: list[Shape]

@dataclass
class Shape:
    label: str
    shape_type: str           # polygon / rectangle / point
    points: list[tuple[float, float]]
```

---

## 七、关键流程

### 流程 1：打开数据集

```
用户点击"打开"
   ↓
弹出文件夹选择对话框
   ↓
gui/workers/scan_worker.py 启动后台线程
   ↓
core/dataset.py::Dataset.scan(path) 扫描文件系统
   ├── 遍历每个类别文件夹
   ├── 收集 images/ 和 labels/ 文件
   ├── 解析 LabelMe JSON
   └── 构建 Dataset 对象
   ↓
缓存索引到本地 SQLite
   ↓
信号回到主线程，刷新 UI
```

### 流程 2：浏览图片

```
用户点击类别树某一项
   ↓
core/dataset.py::get_images_by_category()
   ↓
gui/widgets/thumbnail_grid.py 加载缩略图（懒加载）
   ├── 读取缓存或后台生成
   └── 显示状态角标
   ↓
用户双击 → 打开 detail_view
   ├── 加载原图
   └── 解析对应 JSON 叠加绘制标注
```

---

## 八、开发顺序（建议）

| 阶段 | 内容 | 预计 |
|------|------|------|
| Day 1 上午 | 环境搭建（venv + 依赖）+ 项目骨架 + core/models.py + core/dataset.py | 半天 |
| Day 1 下午 | core/annotation.py + core/stats.py + 单元测试 | 半天 |
| Day 2 上午 | 主窗口 + 菜单栏 + 打开对话框 + 类别树 | 半天 |
| Day 2 下午 | 概览页 + 统计图表 | 半天 |
| Day 3 上午 | 图片浏览器 + 缩略图网格 + 后台缓存 | 半天 |
| Day 3 下午 | 单图详情 + 标注绘制 + 导出功能 | 半天 |
| **完成** | **v1 Demo 跑通** | **3 天** |

---

## 九、扩展性预留

虽然本期不做，但代码层面预留这些扩展点：

1. **core/ 完全独立** — 不 import 任何 PyQt 模块
2. **数据模型用 dataclass** — 方便序列化为 JSON / Pydantic
3. **导出器接口化** — `core/exporter/` 下每个格式一个文件，加新格式不改其他代码
4. **配置外置** — 数据集路径、缓存位置等都走 config.yaml
5. **后台任务统一封装** — 所有耗时操作都通过 worker 包装，方便后期换成异步任务队列

未来如果要升级到完整的 API + 插件架构，只需在 `core/` 之外新增 `api/` 和 `plugins/` 目录，**core 代码不需要重写**。

---

## 十、待确认事项

1. ✅ 技术栈：Python 3.11 + PyQt6
2. ✅ 通用工具，不绑定特定数据集路径
3. ✅ 项目位置：`C:\Users\zq\Desktop\defect_dataset_tool`
4. ⏳ 实际开发时间：等用户决定何时开始
5. ⏳ v1 是否需要打包 exe：未定

---

## 十一、风险与注意点

| 风险 | 应对 |
|------|------|
| 数据集文件多（5000+），扫描慢 | 后台线程 + SQLite 缓存索引 |
| 缩略图加载卡顿 | 懒加载 + 磁盘缓存（diskcache 库） |
| 中文路径 / 中文文件名 | Python 3 原生支持，注意编码 |
| LabelMe JSON 格式变化 | 容错解析，记录解析失败的文件 |
| 标注与图片不配对 | 扫描时检查并标记异常 |
| 大图查看时内存占用高 | 按需加载，不全部读入内存 |

---

**文档版本**：v1.0
**创建日期**：2026-04-07
**状态**：待开发

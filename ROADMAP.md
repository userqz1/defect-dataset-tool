# DataForge 整改方案

核心目标：把 DataForge 从"以节点图为中心"重新定位为**以浏览器为中心、任务类型驱动**的通用图像数据集管理工具，节点图降级为高级用户的可选模板功能。

保留所有架构投资（core/gui 分层、节点抽象、格式网格、主题系统），只调整产品形态和修复底层 bug。

---

## 一、止血：修复底层正确性问题 ✅ 已完成

- ✅ QSS 模板迁移到 string.Template（消除花括号冲突）
- ✅ QPixmap 线程安全（_ImageLoader emit QImage，主线程转 QPixmap）
- ✅ Worker 生命周期（BatchWorker/ScanWorker finished→deleteLater）
- ✅ pyproject.toml + ruff 集成
- ✅ 测试套件 21 tests（pipeline, splitter, annotation）
- ✅ GitHub Actions CI

---

## 二、产品方向修改：浏览器作为一等公民

目标：用户打开应用第一眼看到的应该是**数据**，而不是空画布。

### 2.1 重新设计应用入口

首页从方案管理器改为**数据集主页**：
- 最近打开的数据集列表（带缩略图预览）
- "打开数据集目录" 主操作按钮
- "新建数据集" 按钮
- 右下角小入口 "管线模板"
- `scheme_welcome.py` 改名 `dataset_welcome.py` 并重写

### 2.2 浏览器从节点工作区中独立出来

当前 BrowserView 嵌入在 `pipeline_view._make_datasource_ws()` 内部，作为数据源节点的工作区存在。

需要把 BrowserView **提升为顶级视图**，直接挂在 MainWindow 的导航栏上，不依赖任何方案或节点。

### 2.3 重新组织主窗口导航

导航栏顺序：
```
首页（数据集列表）
浏览器（默认选中，打开数据集后进入）
详情/标注
健康报告
---
管线编辑器（底部）
设置（底部）
```

打开数据集后，浏览器是默认进入的视图，不需要任何"创建方案"的中间步骤。

### 2.4 任务类型驱动整个 UI

`core/task_types.py` 已定义 9 种任务类型，但目前仅用于过滤导出格式。要真正贯穿 UI：

- 打开数据集时弹出 "选择任务类型" 对话框
- 浏览器根据任务类型显示不同信息（检测显示 bbox 数、分类显示标签、分割显示是否有掩码）
- 详情视图根据任务类型决定工具（检测=框工具、分类=标签选择器、分割=多边形工具）

### 2.5 详情视图升级为标注工作台

- 分类任务：右侧面板显示类别选择器，数字键 1-9 直接打标签，A/D 切换图片
- 检测任务：保留现有矩形/多边形工具
- 分割任务：多边形工具优先

至少先把**分类任务的内联打标签**完成。

---

## 三、简化与统一

### 3.1 统一健康检查为单一 Dashboard

新建 `HealthDashboard` 视图，内部三个选项卡：
- **结构** — 格式网格内容，按目标格式分组
- **质量** — 合规性检查、质量问题、覆盖率
- **标注** — 标签覆盖率、每类数量、尺寸分布

每个检查项右侧有 "修复" 按钮，点击跳到对应工具。

### 3.2 浏览器工具栏

浏览器顶部加工具栏：质量检查、去重、增强、划分、导出。每个按钮点击后打开对话框（参数 + 立即执行），不走管线。

### 3.3 导出向导三步设计

- Step 1: 格式选择（图标网格，按任务类型过滤）
- Step 2: 参数配置（划分比例、复制图片、输出目录）
- Step 3: 预览 + 执行（目录结构树、数量分布、开始按钮）

### 3.4 拆分增强视图的双模式

当前 `augment_view` 在 "增强" 和 "变换" 之间切换 — 拆成两个独立入口。

---

## 四、节点图降级为高级模板功能

### 4.1 管线编辑器重新定位

从导航栏底部进入，打开后分两栏：
- 左栏 **模板库**：内置模板 + 用户保存的模板
- 右栏 **画布**：空白或当前编辑的模板

### 4.2 数据源节点变成隐式

打开管线编辑器时已经知道当前数据集是什么（从浏览器跳转过来）。不需要拖数据源节点。

### 4.3 工作区改为侧面板模式

双击节点不再全屏替换画布，而是画布缩到左边 60%，右边 40% 滑出工作区面板。画布始终可见。

### 4.4 节点状态可视化增强

- 失败节点：下游标记为 skipped（灰色虚线）
- 独立分支继续执行
- 颜色：绿=完成，黄=警告，红=错误，灰=未执行

### 4.5 向导到模板的平滑过渡

导出/增强/划分向导的最后一步提供 "另存为管线模板" 链接。

---

## 五、国际化和分发

- i18n：所有用户可见字符串用 `self.tr()` 包裹
- PyInstaller 打包：创建 spec 文件，CI 自动构建三平台
- README：中英双语，含架构图、截图、快速开始

---

## 六、远期扩展（不在主整改范围）

- FiftyOne SDK 兼容层
- 嵌入空间可视化（CLIP/DINOv2 + UMAP）
- 主动学习闭环
- 视频和 3D 数据支持
- 多用户协作

---

## 整改后的产品形态

**80% 普通用户**：打开应用 → 查看最近数据集 → 一键打开 → 进入浏览器 → 看数据/筛选/标注 → 工具栏点击导出 → 3 步完成。整个过程不接触节点图。

**20% 高级用户**：除上述外，可打开管线编辑器，从模板库选一个流程（或从空白构建），应用到当前数据集，保存为模板复用。

**核心理念**：节点图从招牌变暗器 — 平时不亮，亮出来时就是杀手锏。
